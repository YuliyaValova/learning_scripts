& docker container run -t ubuntu top --starts a docker pull to download the Ubuntu image onto your host
docker container exec --allows you to enter a running container's namespaces with a new proces
ssh (port*) --связать новый терминал с контейнером по Ip
docker container ls --get the ID of the running container 
docker container exec -it (container_id*)  bash --перейти в контейнер для выполнения баш скриптов
ps -ef --посмотреть процессы 
если прописать Image без версии, будет скачана последняя, ечли же указать mongo:3.4 например, то загрузится именно эта версия для монгодб
docker container run --detach --publish 8080:80 --name nginx nginx --разместить nginx веб сервер на указанном порте
docker container stop [container id*] --остановить контейнер
docker system prune --удаляет остановленные контейнеры, неиспользуемые тома и сети, зависшие образы
docker image build -t [name*] --build the docker image
docker image ls --get the image list
docker run -p 5001:5000 -d [name*] --run the image
docker container logs [container id] -- gets logs from the container
docker login --log in docker registry
docker tag [local_image_name*] [dockerhub username*]/[image_name*] --Tag the image with your username
docker push [jzaccone/python-hello-world] --The push refers to a repository [docker.io/jzaccone/python-hello-world]
docker swarm init --advertise-addr eth0 --initialize swarm on this node
docker node ls --verify cluster
docker service ls --inspect services
docker service ps [name*] --represents the running instances of a service
curl localhost:80 --send a request to this host
docker service update --replicas=5 --detach=true [name*] --create service replicas
docker service logs [name*] --gets logs for this service

docker service update --image [service:version*] --detach=true [service*] --update service version
You can fine-tune the rolling update by using these options:
--update-parallelism: specifies the number of containers to update immediately (defaults to 1).
--update-delay: specifies the delay between finishing updating a set of containers before moving on to the next set.

docker swarm leave --нода покинет кластер
